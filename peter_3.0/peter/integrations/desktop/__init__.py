"""Desktop control: browsers, bookmarks, media playback, and local places.

Everything here talks to what is already installed on the machine rather than
to a network service, so nothing in this package needs credentials and none of
it can fail because the wifi is down.
"""
